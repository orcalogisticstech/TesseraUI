export function FooterCTA() {
  return (
    <section id="contact" className="section-space">
      <div className="section-wrap surface-card p-8 md:p-12">
        <h2 className="headline text-4xl font-semibold leading-[1.05] md:text-[44px]">READY TO SEE IT RUN?</h2>
        <p className="mt-5 max-w-3xl text-lg" style={{ color: "var(--text-secondary)" }}>
          Start in advisory mode with one facility and one read-only integration. Tessera returns an executable decision plan with measurable impact from shift one.
        </p>
        <a href="#" className="btn-primary mt-8 inline-flex">
          Talk to an operator
        </a>
      </div>
    </section>
  );
}
